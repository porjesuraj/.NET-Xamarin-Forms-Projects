<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Name="page" xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MVVMExercise.View.ContactPage"         
             Title="Contacts">
    <ContentPage.ToolbarItems >
        <ToolbarItem Text="Add" Order="Secondary"  Command="{Binding AddContactCommand}" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout>

            <ListView 
                  	ItemsSource="{Binding Contacts}" 
			SelectedItem="{Binding SelectedContact, Mode=TwoWay}"
			ItemSelected="OnContactSelected"
                      >

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.ContextActions>
                                <MenuItem Text="Delete" IsDestructive="True"
                                         Command="{Binding Source={x:Reference page},Path=ViewModel.DeleteContactCommand}"
                                          CommandParameter="{Binding .}"/>
                            </ViewCell.ContextActions>

                            <Label Text="{Binding FullName}" TextColor="Black" />

                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>


        </StackLayout>
    </ContentPage.Content>
</ContentPage>